//HERE SOME PROPERTIES
export const Model = {
    Lightweight,
    Middleweight,
    Cruiserweight,
    Heavyweight
}

export const State = {
    IDLE,
    LOADING,
    LOADED, DELIVERING,
    DELIVERED,
    RETURNING
}