//HERE SOME PROPERTIES
export const Model = {
    Lightweight,
    Middleweight,
    Cruiserweight,
    Heavyweight,
    ErrorModel
}

export const State = {
    IDLE,
    LOADING,
    LOADED, 
    DELIVERING,
    DELIVERED,
    RETURNING,
    ERROR
}